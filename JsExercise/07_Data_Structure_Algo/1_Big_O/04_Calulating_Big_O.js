// How to calculate complexity line by line bignners

// example:

// Do not try to run it will give you an error message its just for understanding
// how to calculate complexity line by line.
function fun(input) {
    let a = 5;  //O(1)
    a = 5 + 5;  //O(1)
    for (let i = 0; i < array.length; i++) { //O(n)
        let stranger = true;                 //O(n)
        a++;                                 //O(n)
    }
    return a;   //O(1)
}

// BIG O (3 + 4n) i.e; BIG O(n)



